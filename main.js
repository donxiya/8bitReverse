//8-bit reverse
function revers8bit(int){
    let binaryArr = (int >>> 0).toString(2).split("");
    let paddedArr = [0, 0].concat(result).reverse().join("");
    let result = parseInt((result2 + '').replace(/[^01]/gi, ''), 2);
    return result
}
